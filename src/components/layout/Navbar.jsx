const Navbar = () => {
  return (
    <header
      style={{
        position: "fixed",
        top: 0,
        width: "100%",
        zIndex: 100,
        padding: "24px",
      }}
    >
      Navbar
    </header>
  );
};

export default Navbar;